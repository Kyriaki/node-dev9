<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>{{title}}</title>
  </head>
  <body>
    <h1>{{maj title}}</h1>
    <img src="img/{{image}}" alt="{{image}}">

    <ul>
    {{#each users}}
      <li>{{name}}</li>
    {{/each}}
    </ul>

  </body>
  {{!-- <footer>Copyright {{currentYear}}</footer> --}}
  {{>footer}}
</html>
